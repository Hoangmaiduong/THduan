export interface ProjectProgress {
  id: string;
  tt: string | number;
  name: string;
  luyKe: string;
  noiDungTuan: string;
  nhiemVuToi: string;
  danhGiaLocal?: string; // Để lưu tạm đánh giá trên trình duyệt
}
